/// Base URLs and timeouts centrally defined
const String API_BASE_URL = 'https://httpbin.org/';
const Duration CONNECT_TIMEOUT = Duration(seconds: 5);
const Duration RECEIVE_TIMEOUT = Duration(seconds: 3);
